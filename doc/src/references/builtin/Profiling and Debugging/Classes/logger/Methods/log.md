# log
Writes a message at a level you choose.

`void logger::log(log_level level, string message);`

## Arguments:
* log_level level: the severity to record it at.
* string message: the text to record.
